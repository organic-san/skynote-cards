# 語料庫 repo 的 working tree（裡面要有 cards/）
CORPUS_PATH=/srv/corpus
# SQLite 索引。放在語料庫外面，不進版控，隨時可以刪掉重建。
INDEX_PATH=/srv/index.db
PORT=3000
GIT_AUTHOR_NAME=append-cards
GIT_AUTHOR_EMAIL=append-cards@localhost
